#include <stdio.h>

/**
 * main - Entry point
 * Description: printvalueofnstatus;zero,positiveornegative
 * Return: Always 0 (Success)
 */

voidpositive_or_negative(int)
{
	/*myifstatement*/
	if(i==0)
	{
		printf("%diszero\n",i);
	}
	elseif(i>0)
	{
		printf("%dispositive\n",i);
	}
	else
	{
		printf("%disnegative\n",i);
	}
}
